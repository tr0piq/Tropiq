rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Votes: anyone can read and vote (auth optional so vote never fails)
    match /votes/{voteId} {
      allow read: if true;
      allow create: if true;
    }
    // Products collection: anyone can read, authenticated users can write
    match /products/{productId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    // Reviews collection: anyone can read, only authenticated users can create or delete
    match /reviews/{reviewId} {
      allow read: if true;
      allow create, delete: if request.auth != null;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
